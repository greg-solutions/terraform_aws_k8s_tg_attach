### Terraform module for attach all nodes from k8s cluster to AWS Target Group

#### Node must have policy "elasticloadbalancing:RegisterTargets"

##### NOTE: if Target Group ARNs changed, you need manually recreate pods